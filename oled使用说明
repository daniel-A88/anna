
# 7‑oled OLED屏幕实验
视频教程：【STM32入门教程‑2025】第14集 如何在OLED屏幕上挥毫
https://b23.tv/bkB9EPf

## 📦硬件信息
模块：0.96寸 I2C OLED显示屏 SSD1306
通信协议：I2C
供电：**3.3V，不要接5V，容易烧屏幕**

## 🔌硬件接线（I2C1 PB6 PB7）
| OLED模块引脚 | STM32引脚 |
|------|------|
| VDD  | 3.3V |
| GND  | GND  |
| SCK  | PB6(I2C1_SCL) |
| SDA  | PB7(I2C1_SDA) |

> 模块上面印的 SCK = I2C的SCL，SDA不变。

## ⚙️CubeMX配置
1. 开启 I2C1，I2C模式
2. PB6 → I2C1_SCL
3. PB7 → I2C1_SDA
4. 系统时钟配置好，生成HAL工程代码

## 📝代码说明
本工程基于硬件I2C驱动SSD1306 OLED。
- `oled.c / oled.h`：OLED底层驱动，初始化、发送命令、发送数据
- `oled_font.h`：字模库，存放ASCII字符点阵
- `main.c`：调用示例
## 🖥️波特律动（PCtoLCD2002）取模软件使用
 > 视频里面用来生成OLED字模点阵的工具，用来生成 `oled_font.h` 字库文件
 ### 软件关键设置（必须和视频保持一致）
 1. 选项设置
 - **点阵格式：阴码**
 - **取模方式：逐行式**
 - **输出：C语言数组**
 - 取消：“逆向输出”、“低位在前”
 - 字体：一般选择 `ASCII 8*16` 或者 `6*8`
 2. 使用步骤
 1）输入想要的字符、英文、数字
 2）点击【生成字模】
 3）复制生成的大数组
 4）粘贴到 `oled_font.h` 头文件里面
 5）在驱动代码引用这个数组，OLED就可以显示对应字符
 > ⚠️取模参数一旦改错，屏幕就会出现乱码、字符错位、雪花。参数必须和代码里面字模大小匹配。
 ## 📝代码说明
 本工程基于硬件I2C驱动SSD1306 OLED。
 - `oled.c / oled.h`：OLED底层驱动，初始化、发送命令、发送数据
 - `oled_font.h`：字模库，波特律动生成的字符点阵
 - `main.c`：调用示例

### 重要地址说明
OLED I2C设备地址二选一，屏幕不同地址不一样：
- 0x78 （最常见）
- 0x7A

> HAL库调用的时候地址需要左移1位：`0x78 << 1`

### 核心函数
```c
OLED_Init();                 // OLED屏幕初始化
OLED_Clear();                // 清屏
OLED_ShowString(0,0,"test");// 在x=0,y=0位置显示字符串
OLED_Refresh();              // 更新显存到屏幕，必须调用才会显示

若有报错，直接点击报错的地方，可能是你修改的文件不在编译内
